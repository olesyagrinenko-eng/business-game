# Render Blueprint: один клик деплой из репозитория
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: business-game
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT app:app
